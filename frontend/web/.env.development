# 本地开发环境
NODE_ENV=development

# API 接口地址 (通过 Vite 代理)
VITE_APP_API_BASE_URL=/api

# WebSocket 地址 (通过 Vite 代理)
VITE_WS_URL=/ws

# 默认组织ID和用户ID（开发环境）
VITE_DEFAULT_ORG_ID=default-org
VITE_DEFAULT_USER_ID=default-user
