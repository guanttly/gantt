# M08 — AI 服务

> **所属阶段**: 阶段三（第 9-11 周）  
> **前置依赖**: M06（排班管道）、M05（规则引擎）  
> **交付物**: AI 适配层 + 对话交互 + 意图识别 + AI 辅助排班 Step

---

## 一、目标

封装 AI Provider 适配层，实现多 LLM 后端统一接口；提供排班领域的意图识别、规则解析、智能选人等能力，作为 Pipeline Step 集成到排班流程。

---

## 二、架构

```
┌── AI Service (内部模块) ─────────────────────┐
│                                              │
│  ┌─────────────┐   ┌──────────────────────┐  │
│  │  Provider    │   │   Domain AI Logic    │  │
│  │  Adapter     │   │                      │  │
│  │ ┌─────────┐  │   │ ├─ IntentParser     │  │
│  │ │ OpenAI  │  │   │ ├─ RuleParser       │  │
│  │ ├─────────┤  │   │ ├─ ChatHandler      │  │
│  │ │ 百炼    │  │   │ └─ AISelectStep     │  │
│  │ ├─────────┤  │   └──────────────────────┘  │
│  │ │ Ollama  │  │                              │
│  │ └─────────┘  │          HTTP                │
│  └──────┬───────┘     ───────────────►         │
│         │              外部 LLM API            │
│         ▼                                      │
│  ┌─────────────┐                               │
│  │ Quota Mgr   │  ← 按租户配额/计费            │
│  └─────────────┘                               │
└──────────────────────────────────────────────┘
```

---

## 三、AI Provider 适配层

### 3.1 Provider 接口

```go
// internal/ai/provider.go

type Provider interface {
    // Chat 单轮/多轮对话
    Chat(ctx context.Context, req ChatRequest) (*ChatResponse, error)
    // ChatStream 流式输出
    ChatStream(ctx context.Context, req ChatRequest) (<-chan StreamChunk, error)
    // Name 返回 Provider 名称
    Name() string
}

type ChatRequest struct {
    Model       string            `json:"model"`
    Messages    []Message         `json:"messages"`
    Temperature float64           `json:"temperature,omitempty"`
    MaxTokens   int               `json:"max_tokens,omitempty"`
    Tools       []ToolDefinition  `json:"tools,omitempty"`  // Function Calling
}

type Message struct {
    Role    string `json:"role"`    // system / user / assistant / tool
    Content string `json:"content"`
}

type ChatResponse struct {
    Content    string       `json:"content"`
    ToolCalls  []ToolCall   `json:"tool_calls,omitempty"`
    Usage      TokenUsage   `json:"usage"`
    FinishReason string    `json:"finish_reason"`
}

type TokenUsage struct {
    PromptTokens     int `json:"prompt_tokens"`
    CompletionTokens int `json:"completion_tokens"`
    TotalTokens      int `json:"total_tokens"`
}
```

### 3.2 已实现适配器迁移

从 `pkg/ai/` 迁移并重构以下适配器：

| 适配器 | 源文件 | 目标 | 改动 |
|--------|--------|------|------|
| OpenAI 兼容 | `pkg/ai/openai.go` | `internal/ai/openai_provider.go` | 统一 Provider 接口 |
| 阿里百炼 | `pkg/ai/bailian.go` | `internal/ai/bailian_provider.go` | 统一 Provider 接口 |
| Ollama | `pkg/ai/ollama.go` | `internal/ai/ollama_provider.go` | 统一 Provider 接口 |
| 本地模型 | `pkg/ai/local.go` | `internal/ai/local_provider.go` | 评估是否保留 |

### 3.3 Provider 工厂

```go
// internal/ai/factory.go

type Factory struct {
    providers map[string]Provider
}

func NewFactory(cfg config.AIConfig) *Factory {
    f := &Factory{providers: make(map[string]Provider)}
    if cfg.OpenAI.Enabled {
        f.providers["openai"] = NewOpenAIProvider(cfg.OpenAI)
    }
    if cfg.Bailian.Enabled {
        f.providers["bailian"] = NewBailianProvider(cfg.Bailian)
    }
    if cfg.Ollama.Enabled {
        f.providers["ollama"] = NewOllamaProvider(cfg.Ollama)
    }
    return f
}

func (f *Factory) Get(name string) (Provider, error) {
    p, ok := f.providers[name]
    if !ok {
        return nil, fmt.Errorf("unknown AI provider: %s", name)
    }
    return p, nil
}

func (f *Factory) Default() Provider {
    // 按优先级返回第一个可用的 Provider
}
```

---

## 四、配额管理

### 4.1 数据表

```sql
CREATE TABLE ai_quotas (
    id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    org_node_id   BIGINT UNSIGNED NOT NULL,
    provider      VARCHAR(32)     NOT NULL DEFAULT 'openai',
    monthly_limit INT             NOT NULL DEFAULT 100000,  -- 月 token 上限
    used_tokens   INT             NOT NULL DEFAULT 0,
    reset_at      DATETIME        NOT NULL,                 -- 下次重置时间
    created_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    UNIQUE KEY uk_org_provider (org_node_id, provider)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE ai_usage_logs (
    id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    org_node_id   BIGINT UNSIGNED NOT NULL,
    user_id       BIGINT UNSIGNED NOT NULL,
    provider      VARCHAR(32)     NOT NULL,
    model         VARCHAR(64)     NOT NULL,
    prompt_tokens INT             NOT NULL DEFAULT 0,
    completion_tokens INT         NOT NULL DEFAULT 0,
    purpose       VARCHAR(64)     NOT NULL,  -- chat / intent / rule_parse / ai_select
    created_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_org_time (org_node_id, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 4.2 配额检查中间件

```go
func (q *QuotaManager) CheckAndDeduct(ctx context.Context, orgNodeID uint64, usage TokenUsage) error {
    // 1. 查询当前月配额
    // 2. 检查 used_tokens + usage.TotalTokens <= monthly_limit
    // 3. 原子递增 used_tokens（乐观锁 UPDATE ... WHERE used_tokens = old_val）
    // 4. 写入 ai_usage_logs
    // 5. 超额返回 errors.QuotaExceeded
}
```

---

## 五、领域 AI 能力

### 5.1 意图识别

```go
// internal/ai/intent/parser.go

type Intent struct {
    Action     string            `json:"action"`      // create_schedule / adjust / query / chat
    Entities   map[string]string `json:"entities"`    // date_range, department, shift, employee...
    Confidence float64           `json:"confidence"`
}

func (p *IntentParser) Parse(ctx context.Context, userInput string) (*Intent, error) {
    systemPrompt := `你是排班助手。根据用户输入，提取意图和实体。
可用意图: create_schedule, adjust_schedule, query_schedule, query_rule, chat
可用实体: date_range, department, shift_name, employee_name, rule_type
以JSON格式返回: {"action":"...","entities":{...},"confidence":0.0~1.0}`
    
    resp, err := p.provider.Chat(ctx, ChatRequest{
        Messages: []Message{
            {Role: "system", Content: systemPrompt},
            {Role: "user", Content: userInput},
        },
        Temperature: 0.1,
    })
    // 解析 JSON 响应
}
```

### 5.2 规则解析

```go
// internal/ai/ruleparse/parser.go

// ParseNaturalLanguageRule 将自然语言描述转为规则配置 JSON
func (p *RuleParser) Parse(ctx context.Context, description string) (*RuleConfig, error) {
    systemPrompt := `你是排班规则配置助手。
将用户的自然语言规则描述转为标准JSON格式。

支持的规则类型:
- exclusive: 互斥排班，config: {"shifts": ["A","B"]}
- max_consecutive: 最大连续天数，config: {"max_days": N}
- required: 固定排班，config: {"employee_id":"...", "shift_id":"...", "dates":["..."]}
- preference: 偏好权重，config: {"employee_id":"...", "prefer_shifts":["..."]}

返回: {"type":"...","name":"...","config":{...}}`
    
    resp, err := p.provider.Chat(ctx, ChatRequest{
        Messages: []Message{
            {Role: "system", Content: systemPrompt},
            {Role: "user", Content: description},
        },
        Temperature: 0.1,
    })
    // 解析并验证 JSON
}
```

### 5.3 AI 选人 Step

```go
// internal/scheduling/step/ai_select.go

// AISelectStep 使用 AI 为「填充阶段」选择最优候选人
type AISelectStep struct {
    provider ai.Provider
}

func (s *AISelectStep) Name() string { return "ai_select" }

func (s *AISelectStep) Execute(ctx context.Context, state *ScheduleState) error {
    // 1. 构建 prompt：传入空缺班次列表 + 候选员工 + 规则摘要
    // 2. 请求 LLM 返回分配方案 JSON
    // 3. 解析响应为 assignment 列表
    // 4. 逐条校验规则
    // 5. 写入 state.Assignments（source = "ai"）
    
    prompt := s.buildPrompt(state)
    resp, err := s.provider.Chat(ctx, ai.ChatRequest{
        Messages: []ai.Message{
            {Role: "system", Content: aiSelectSystemPrompt},
            {Role: "user", Content: prompt},
        },
        Temperature: 0.3,
    })
    if err != nil {
        return fmt.Errorf("ai select: %w", err)
    }
    
    assignments, err := s.parseAssignments(resp.Content)
    if err != nil {
        return fmt.Errorf("ai select parse: %w", err)
    }
    
    // 校验每条 AI 分配
    for _, a := range assignments {
        violations := state.RuleChecker.Check(a, state)
        if len(violations) == 0 {
            a.Source = "ai"
            state.Assignments = append(state.Assignments, a)
        } else {
            state.Warnings = append(state.Warnings, violations...)
        }
    }
    return nil
}
```

### 5.4 对话交互

```go
// internal/ai/chat/handler.go

type ChatHandler struct {
    intentParser *intent.IntentParser
    ruleParser   *ruleparse.RuleParser
    provider     ai.Provider
    scheduleService *scheduling.Service
}

func (h *ChatHandler) Handle(ctx context.Context, msg UserMessage) (*BotResponse, error) {
    // 1. 意图识别
    intent, _ := h.intentParser.Parse(ctx, msg.Content)
    
    switch intent.Action {
    case "create_schedule":
        // 提取日期范围、科室等实体，引导创建流程
        return h.handleCreateSchedule(ctx, intent)
    case "adjust_schedule":
        // 提取调整意图，调用调整 Pipeline
        return h.handleAdjustSchedule(ctx, intent)
    case "query_schedule":
        // 查询排班数据，格式化返回
        return h.handleQuerySchedule(ctx, intent)
    default:
        // 通用闲聊
        return h.handleGenericChat(ctx, msg)
    }
}
```

---

## 六、API 接口

```
POST   /api/v1/ai/chat               # 对话交互
POST   /api/v1/ai/parse-rule          # 自然语言→规则配置
GET    /api/v1/ai/quota               # 查询当前配额
GET    /api/v1/ai/usage               # 查询使用记录
```

**POST /ai/chat 请求**:
```json
{
  "message": "帮我排下周一到周五急诊科的班",
  "conversation_id": "conv-001"
}
```

**POST /ai/chat 响应**:
```json
{
  "reply": "好的，我来为急诊科安排3/24-3/28的排班。当前有12名可排班员工，检测到3条排班规则。\n\n请确认是否开始生成？",
  "intent": "create_schedule",
  "entities": {
    "department": "急诊科",
    "date_range": "2026-03-24~2026-03-28"
  },
  "actions": [
    { "type": "confirm", "label": "开始生成", "payload": { "pipeline": "ai_assisted" } }
  ]
}
```

---

## 七、配置

```yaml
# config/common.yml → ai 段
ai:
  default_provider: openai
  openai:
    enabled: true
    api_key: "${OPENAI_API_KEY}"
    base_url: "https://api.openai.com/v1"
    model: "gpt-4o"
    timeout: 30s
  bailian:
    enabled: false
    api_key: "${BAILIAN_API_KEY}"
    model: "qwen-plus"
  ollama:
    enabled: false
    base_url: "http://localhost:11434"
    model: "qwen2.5:14b"
  quota:
    default_monthly_tokens: 100000
```

---

## 八、目录结构

```
internal/
├── ai/
│   ├── provider.go            # Provider 接口定义
│   ├── factory.go             # Provider 工厂
│   ├── openai_provider.go     # OpenAI 适配器
│   ├── bailian_provider.go    # 百炼适配器
│   ├── ollama_provider.go     # Ollama 适配器
│   ├── quota/
│   │   ├── manager.go         # 配额管理
│   │   └── repository.go      # 配额数据访问
│   ├── intent/
│   │   └── parser.go          # 意图识别
│   ├── ruleparse/
│   │   └── parser.go          # 规则解析
│   └── chat/
│       └── handler.go         # 对话处理
```

---

## 九、验收标准

| 验收项 | 通过条件 |
|--------|---------|
| Provider 适配 | OpenAI + 百炼至少一个可正常对话 |
| 意图识别 | 排班相关意图准确率 > 85%（10 条测试用例） |
| 规则解析 | 自然语言→规则 JSON 成功率 > 80% |
| AI 选人 | AI 分配结果通过规则校验率 > 90% |
| 配额管理 | 超额时返回 429，不发起 LLM 请求 |
| 使用记录 | 每次 AI 调用均记录 token 消耗 |
| 对话交互 | 支持创建/调整/查询意图的引导式交互 |
